{
  "name": "quiz-site",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:config": "node scripts/build-config.js",
    "prestart": "npm run build:config",
    "start": "node server.js"
  }
}
